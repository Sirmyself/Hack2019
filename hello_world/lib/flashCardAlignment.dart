import 'package:flutter/material.dart';
import 'main.dart';

class FlashCardAlignment extends StatelessWidget
{
  final int cardNum;
  FlashCardAlignment(this.cardNum);

  @override
  Widget build(BuildContext context)
  {
    return flashCard;
  }
}